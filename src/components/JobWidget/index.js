import React, { PureComponent } from "react";
export default class JobWidgetControl extends PureComponent {
  onChange = (event, editor) => {
    //   const data = editor.getData();
    //   this.props.onChange(data);
  };

  render() {
    return <h1>Job widget test</h1>;
  }
}
